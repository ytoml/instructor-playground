# InstructorPlayground

# What?
Playground for `instructor` library + Anthropic.

Use `LiteLLM` proxy.
You need to set the `ANTHROPIC_API_KEY` and `ANTHRPOIC_MODEL` environment variables to run the proxy.



### Formatter, Linter
```shell
make fmt
make lint
```

### Test
```shell
make test
```


